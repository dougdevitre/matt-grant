// Twitter card mirrors the per-pillar Open Graph card (same renderer, same params).
export { default, alt, size, contentType, generateStaticParams } from "./opengraph-image";
export const runtime = "nodejs";
