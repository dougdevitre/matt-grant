// Twitter card mirrors the Open Graph card (same renderer) so X/Twitter shows the
// game-specific preview instead of falling back to the generic root card.
export { default, alt, size, contentType } from "./opengraph-image";
export const runtime = "nodejs";
